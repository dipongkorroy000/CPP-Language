#include <iostream>
using namespace std;
int main()
{
    // char grade = 'a';
    // int value = grade;
    // cout << value << endl;

    // double price = 100.999;
    // int newPrice = (int)price; // result 100.999 - 100---
    // cout << newPrice << endl;

    //random number print--
    // int number;
    // cout << number << endl;

    // int age;
    // cout << "enter your age : ";
    // cin >> age;

    // cout << "your age is : " << age << endl;

        //arithmetic operators--
    // int a = 9, b = 5;
    // cout << "sum = " << (a + b) << endl;
    // cout << "sub = " << (a - b) << endl;
    // cout << "multiply = " << (a * b) << endl;
    // cout << "quotient = " << (a / b) << endl;
    // cout << "remainder = " << (a % b) << endl;

    // need absolute result--
    // int n1 = 10;
    // double n2 = 3;
    // cout << "quotient = " << (n1 / n2) << endl;
    //alternative--
    // cout << "quotient = " << (10 / (double)3) << endl;

        // relational operators---
    // cout << (4 < 5) << endl; // true -> 1;
    // cout << (4 > 5) << endl; // false -> 0;
    // cout << (4 == 4) << endl; // true -> 1;
    // cout << (4 == 5) << endl; // false -> 0;
    // cout << (4 != 5) << endl; // true -> 1;
    // cout << (4 != 4) << endl; // false -> 0;
    
    // cout << !(4 != 4) << endl; // true -> 1;

        //logical operators--
    // cout << ((9 > 7) && (4 < 5)) << endl; // true -> 1;
    // cout << ((9 > 7) && (4 > 5)) << endl; // false -> 0;
    // cout << ((9 > 7) || (4 > 5)) << endl; // true -> 0;
    // cout << ((9 < 7) || (4 > 5)) << endl; // false -> 0;

        /*
        Unary operators--
        1. Increment operator--
            a. pre Increment ++a;
            b. post Increment a++;
        2. Decrement operator--
            a. pre Decrement --a;
            b. post Decrement a--;
        */

    //Qus--
    // int a, b;
    // cout << "enter a : ";
    // cin >> a;

    // cout << "enter b : ";
    // cin >> b;

    // int sum = a + b;
    // cout << "sum = " << sum << endl;
    return 0;
}